import { describe, it, expect } from 'vitest';

// Placeholder chứng minh test runner chạy (task 1.1). Test thật cho drift nằm ở
// components/__tests__/ChatWidget.drift.test.tsx.
describe('test runner', () => {
  it('chạy được', () => {
    expect(1 + 1).toBe(2);
  });
});
